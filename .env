# Set environment variables to customize
WORKER_COUNT=4              # Cluster workers (default: CPU cores, max 4)
SCRAPING_WORKERS=2          # Scraping threads per worker
MAX_CONCURRENT_REQUESTS=3   # Max concurrent requests per worker
WORKER_MEMORY_LIMIT=512     # Memory limit per worker (MB)
